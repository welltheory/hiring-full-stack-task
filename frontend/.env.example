# Backend API URL
VITE_API_URL=http://localhost:3031

# Authenticated User ID
# This simulates authentication
# In production, this would come from a real auth system (JWT, sessions, etc.)
VITE_CURRENT_USER_ID=cldemoalice00000000000000
